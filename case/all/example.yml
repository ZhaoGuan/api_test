SOURCE:
  URL_PATH: /v1/app/gbwa/config/theme/fonts/list
  METHOD:
        POST
  DATA_TYPE :
        ONLY
  online:
    URL:
    HEADERS:
      TYPE:
        NORMAL
      DATA:
    PARAMS:
      TYPE:
        JOIN
      DATA:
    METHOD:
        POST
    BODY:
      TYPE:
        JSON
      DATA:
        {"1":2,"3":4}
  dev:
    URL:
    HEADERS:
      TYPE:
        NORMAL
      DATA:
    PARAMS:
      TYPE:
        JOIN
      DATA:
    METHOD:
        POST
    BODY:
      TYPE:
        JSON
      DATA:
        {"1":2,"3":4}
ASSERT :
    DATA_FORMAT:
        TYPE:
          ONLY
        DATA:
    DATA_CONTENT:
       online:
       test:
     ANOTHER_ASSSERT:
        - TYPE :
          DATA :
    RESPONSE_HEADER:
